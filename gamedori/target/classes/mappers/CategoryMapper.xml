<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="game.dori.mapper.categoryMapper">

	<!-- 카테고리 호출 -->
	<select id="category" resultType="game.dori.vo.CATEGORY_VO">
		select cate_code, cate_name, cate_refcode from category_tb
			start with cate_refcode in connect by prior cate_code = cate_refcode;
	</select>
</mapper>